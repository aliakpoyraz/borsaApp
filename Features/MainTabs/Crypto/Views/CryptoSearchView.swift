import SwiftUI

struct CryptoSearchView: View {
    var body: some View {
        NavigationStack {
            Text("Kripto arama ekranı daha sonra geliştirilecek.")
                .foregroundStyle(.secondary)
                .navigationTitle("Kripto Arama")
        }
    }
}
